{
  "name": "cs-ecommerce",
  "private": true,
  "workspaces": [
    "backend",
    "apps/storefront",
    "apps/super-admin"
  ],
  "scripts": {
    "dev": "concurrently -n api,shop,admin -c blue,green,magenta \"npm run dev -w backend\" \"npm run dev -w storefront\" \"npm run dev -w super-admin\"",
    "dev:backend": "npm run dev -w backend",
    "dev:storefront": "npm run dev -w storefront",
    "dev:super-admin": "npm run dev -w super-admin",
    "migrate": "npm run migrate -w backend",
    "migrate:rollback": "npm run migrate:rollback -w backend",
    "build": "npm run build -w backend && npm run build -w storefront && npm run build -w super-admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
